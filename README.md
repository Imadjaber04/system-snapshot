# System Health Snapshot (Python)

A beginner-friendly Python script that prints basic system info and disk usage,
then saves the output as a timestamped report file.

## Features
- OS and machine info
- Disk usage (total/used/free + used %)
- Saves a report automatically as a `.txt` file

## How to run
python3 src/main.py
